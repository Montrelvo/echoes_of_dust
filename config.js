// --- Constants ---
const BUILDING_COSTS = {
    shelter: { scrap: 10 }
};
const POPULATION_PER_SHELTER = 2;
// Idle production constants are now within gameState.idleProduction objects
// If other truly global constants are needed, they can be added here.